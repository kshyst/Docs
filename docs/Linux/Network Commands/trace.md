## Traceroute

```shell
traceroute digikala.com
```

> In some routers, the ping traffic is blocked, and you will see * * * at some steps because those servers are blocking ICMP traffic.


## Tracepath

```shell
tracepath digikala.com
```